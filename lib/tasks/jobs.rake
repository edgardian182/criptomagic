namespace :candles do
  desc 'cron job for candle creation'
  # task new: :environment do
  #   b = Binance.last
  #   Coin.new_candle(b.id, '15m')
  # end
  #
  # task generate: :environment do
  #   b = Binance.last
  #   Coin.generate_candle(b.id, '15m')
  # end
end
